{
  "name": "expostands-panama",
  "type": "module",
  "version": "1.0.0",
  "description": "ExpoStands Panamá - Fabricación de stands, exhibidores y mobiliario comercial",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node scripts/inline-css.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.0.0",
    "esbuild": "^0.28.0"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
